# Routes Sozlash - 2026-02-27 15:21

## 📋 Vazifa
Barcha yangi route'larni sozlash

## ✅ Bajarilgan ishlar

### 1. Import Qo'shildi
- ✅ TableController
- ✅ WaiterController
- ✅ KitchenController
- ✅ CashierController

### 2. Admin Routes (Tables)
**Route'lar:**
- `GET /admin/tables` - Stollar ro'yxati (TableController@index)
- `POST /admin/tables` - Yangi stol yaratish (TableController@store)
- `PUT /admin/tables/{table}` - Stol yangilash (TableController@update)
- `DELETE /admin/tables/{table}` - Stol o'chirish (TableController@destroy)

**Middleware:** auth, verified, admin

### 3. Waiter Routes (Ofitsiant)
**Prefix:** `/waiter`  
**Name prefix:** `waiter.`  
**Middleware:** auth, verified, waiter

**Route'lar:**
- `GET /waiter/tables` - Barcha stollar (WaiterController@tables)
- `GET /waiter/tables/{table}/order` - Buyurtma yaratish sahifasi (WaiterController@createOrder)
- `POST /waiter/orders` - Buyurtma yaratish (WaiterController@storeOrder)
- `GET /waiter/orders/active` - Faol buyurtmalar (WaiterController@activeOrders)
- `PUT /waiter/orders/{order}/delivered` - Yetkazildi deb belgilash (WaiterController@markDelivered)

### 4. Kitchen Routes (Oshxona)
**Prefix:** `/kitchen`  
**Name prefix:** `kitchen.`  
**Middleware:** auth, verified, kitchen

**Route'lar:**
- `GET /kitchen/dashboard` - Oshxona dashboard (KitchenController@dashboard)
- `GET /kitchen/orders` - Barcha buyurtmalar (KitchenController@orders)
- `PUT /kitchen/orders/{order}/confirm` - Qabul qilish (KitchenController@confirm)
- `PUT /kitchen/orders/{order}/preparing` - Tayyorlashni boshlash (KitchenController@preparing)
- `PUT /kitchen/orders/{order}/ready` - Tayyor deb belgilash (KitchenController@ready)

### 5. Cashier Routes (Kassa)
**Prefix:** `/cashier`  
**Name prefix:** `cashier.`  
**Middleware:** auth, verified, cashier

**Route'lar:**
- `GET /cashier/dashboard` - Kassa dashboard (CashierController@dashboard)
- `GET /cashier/orders/pending-payment` - To'lov kutilayotgan buyurtmalar (CashierController@pendingPayments)
- `POST /cashier/orders/{order}/payment` - Bitta buyurtma uchun to'lov (CashierController@processPayment)
- `POST /cashier/sessions/{session}/payment` - Butun sessiya uchun to'lov (CashierController@sessionPayment)

## 🔍 Texnik Tafsilotlar

### Route Groups
- Har bir role uchun alohida route group
- Prefix va name prefix bilan tashkil etilgan
- Middleware chain: auth → verified → role middleware

### Route Naming
- Admin: `admin.tables.index`, `admin.tables.store`, etc.
- Waiter: `waiter.tables`, `waiter.order.create`, `waiter.order.store`, etc.
- Kitchen: `kitchen.dashboard`, `kitchen.orders`, `kitchen.order.confirm`, etc.
- Cashier: `cashier.dashboard`, `cashier.orders.pending`, `cashier.payment.process`, etc.

### Middleware Protection
- Barcha route'lar authentication talab qiladi
- Email verification talab qiladi
- Role-based access control

## 📝 Eslatmalar
- Barcha route'lar tayyor
- Keyingi qadam: TelegramService yangilash
- Frontend sahifalar keyinroq yaratiladi

## ⏱️ Vaqt
**Boshlanish:** 15:20  
**Tugash:** 15:21  
**Davomiylik:** ~1 daqiqa
